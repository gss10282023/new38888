# Django Settings
SECRET_KEY=django-insecure-change-this-in-production-$(openssl rand -base64 32)
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DB_NAME=btf_db
DB_USER=btf_user
DB_PASSWORD=btf_password_2025
DB_HOST=localhost
DB_PORT=5432

# Redis Configuration
REDIS_URL=redis://127.0.0.1:6379/1

# CORS Settings (前端开发服务器地址)
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5179,http://127.0.0.1:5179

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME_HOURS=1
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email Configuration (Gmail SMTP 示例 - 使用应用专用密码)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=youremail@gmail.com
EMAIL_HOST_PASSWORD=your_app_password_here
DEFAULT_FROM_EMAIL=youremail@gmail.com

# Vultr Object Storage (S3 Compatible - Step 5 再配置)
# VULTR_ACCESS_KEY=your_vultr_access_key
# VULTR_SECRET_KEY=your_vultr_secret_key
# VULTR_BUCKET_NAME=btf-storage
# VULTR_S3_ENDPOINT=https://sjc1.vultrobjects.com
# VULTR_S3_REGION=sjc1
